#include <iostream>

using namespace std;

int main() {

    int a , b, c, d,e;

    cin >> a >> b >> c >> d >> e;

cout << "The average of a, b, c, d and e is: " << (a + b + c + d + e) / 5 << endl;

    return 0;
}